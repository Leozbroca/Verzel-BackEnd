export interface authenticationData {
    id: string;
    role: string;
  }

  export interface user {
    id: string;
    nome: string;
    email: string;
    senha: string;
  }

  export interface car {
    nome: string;
    email: string;
    marca: string;
    modelo: string;
    ano: number;
    foto: string
  }
  